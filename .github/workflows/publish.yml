name: Publish

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
      
    - name: Build
      run: |
        npm install
        npm run build
    - name: Rename css file
      run: | 
        cd build/static/css
        mv main.*.css /home/file-store/main.groupslist.css
    - name: Rename js file
      run: |
        cd .
        cd build/static/js
        mv main.*.js /home/file-store/main.groupslist.jss
    - name: Checkout storage repository
      uses: actions/checkout@v4.2.2
      with:
        repository: scdantealighieri/public-store
      
    - name: Move file to public store repository
      run: |
        mv /home/file-store/main.groupslist.css ./groups-list/main.groupslist.css
        mv /home/file-store/main.groupslist.js ./groups-list/main.groupslist.js

    - name: Push to github
      run: |
        git commit -a
        git push main --force 

        
